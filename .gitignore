# Секреты — никогда не попадают в git
.env

# Логи
logs/

# Состояние фоновой проверки (этап 5)
state/

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Данные, которые агент меняет в рантайме (монтируются как volume)
data/rules.md
data/*.json
